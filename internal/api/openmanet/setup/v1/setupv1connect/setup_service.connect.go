// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: openmanet/setup/v1/setup_service.proto

package setupv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/openmanet/openmanetd/internal/api/openmanet/setup/v1"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SetupServiceName is the fully-qualified name of the SetupService service.
	SetupServiceName = "openmanet.setup.v1.SetupService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SetupServiceGetSetupStatusProcedure is the fully-qualified name of the SetupService's
	// GetSetupStatus RPC.
	SetupServiceGetSetupStatusProcedure = "/openmanet.setup.v1.SetupService/GetSetupStatus"
	// SetupServiceApplySetupProcedure is the fully-qualified name of the SetupService's ApplySetup RPC.
	SetupServiceApplySetupProcedure = "/openmanet.setup.v1.SetupService/ApplySetup"
)

// SetupServiceClient is a client for the openmanet.setup.v1.SetupService service.
type SetupServiceClient interface {
	// GetSetupStatus reports whether the wizard is reachable on this
	// device and provides the hardware context (radios, ethernet ports,
	// hostname) the wizard needs to render its first screen. Always
	// exempt from auth so the frontend can poll it before the user has
	// any credentials.
	GetSetupStatus(context.Context, *emptypb.Empty) (*v1.GetSetupStatusResponse, error)
	// ApplySetup atomically applies a complete MeshNodeProfile. The RPC
	// is server-streaming: each phase emits STATUS_STARTED on entry and
	// STATUS_DONE / STATUS_FAILED on exit, plus a terminal event carrying
	// ApplySetupResult.
	//
	// The terminal event is emitted BEFORE service reloads so the
	// frontend receives a clean success / failure signal even when the
	// network reconfiguration severs the streaming connection. Frontend
	// clients should treat an abrupt stream end (no terminal event seen)
	// as ambiguous and fall back to polling GetSetupStatus.
	ApplySetup(context.Context, *v1.ApplySetupRequest) (*connect.ServerStreamForClient[v1.ApplySetupResponse], error)
}

// NewSetupServiceClient constructs a client for the openmanet.setup.v1.SetupService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSetupServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SetupServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	setupServiceMethods := v1.File_openmanet_setup_v1_setup_service_proto.Services().ByName("SetupService").Methods()
	return &setupServiceClient{
		getSetupStatus: connect.NewClient[emptypb.Empty, v1.GetSetupStatusResponse](
			httpClient,
			baseURL+SetupServiceGetSetupStatusProcedure,
			connect.WithSchema(setupServiceMethods.ByName("GetSetupStatus")),
			connect.WithClientOptions(opts...),
		),
		applySetup: connect.NewClient[v1.ApplySetupRequest, v1.ApplySetupResponse](
			httpClient,
			baseURL+SetupServiceApplySetupProcedure,
			connect.WithSchema(setupServiceMethods.ByName("ApplySetup")),
			connect.WithClientOptions(opts...),
		),
	}
}

// setupServiceClient implements SetupServiceClient.
type setupServiceClient struct {
	getSetupStatus *connect.Client[emptypb.Empty, v1.GetSetupStatusResponse]
	applySetup     *connect.Client[v1.ApplySetupRequest, v1.ApplySetupResponse]
}

// GetSetupStatus calls openmanet.setup.v1.SetupService.GetSetupStatus.
func (c *setupServiceClient) GetSetupStatus(ctx context.Context, req *emptypb.Empty) (*v1.GetSetupStatusResponse, error) {
	response, err := c.getSetupStatus.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// ApplySetup calls openmanet.setup.v1.SetupService.ApplySetup.
func (c *setupServiceClient) ApplySetup(ctx context.Context, req *v1.ApplySetupRequest) (*connect.ServerStreamForClient[v1.ApplySetupResponse], error) {
	return c.applySetup.CallServerStream(ctx, connect.NewRequest(req))
}

// SetupServiceHandler is an implementation of the openmanet.setup.v1.SetupService service.
type SetupServiceHandler interface {
	// GetSetupStatus reports whether the wizard is reachable on this
	// device and provides the hardware context (radios, ethernet ports,
	// hostname) the wizard needs to render its first screen. Always
	// exempt from auth so the frontend can poll it before the user has
	// any credentials.
	GetSetupStatus(context.Context, *emptypb.Empty) (*v1.GetSetupStatusResponse, error)
	// ApplySetup atomically applies a complete MeshNodeProfile. The RPC
	// is server-streaming: each phase emits STATUS_STARTED on entry and
	// STATUS_DONE / STATUS_FAILED on exit, plus a terminal event carrying
	// ApplySetupResult.
	//
	// The terminal event is emitted BEFORE service reloads so the
	// frontend receives a clean success / failure signal even when the
	// network reconfiguration severs the streaming connection. Frontend
	// clients should treat an abrupt stream end (no terminal event seen)
	// as ambiguous and fall back to polling GetSetupStatus.
	ApplySetup(context.Context, *v1.ApplySetupRequest, *connect.ServerStream[v1.ApplySetupResponse]) error
}

// NewSetupServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSetupServiceHandler(svc SetupServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	setupServiceMethods := v1.File_openmanet_setup_v1_setup_service_proto.Services().ByName("SetupService").Methods()
	setupServiceGetSetupStatusHandler := connect.NewUnaryHandlerSimple(
		SetupServiceGetSetupStatusProcedure,
		svc.GetSetupStatus,
		connect.WithSchema(setupServiceMethods.ByName("GetSetupStatus")),
		connect.WithHandlerOptions(opts...),
	)
	setupServiceApplySetupHandler := connect.NewServerStreamHandlerSimple(
		SetupServiceApplySetupProcedure,
		svc.ApplySetup,
		connect.WithSchema(setupServiceMethods.ByName("ApplySetup")),
		connect.WithHandlerOptions(opts...),
	)
	return "/openmanet.setup.v1.SetupService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SetupServiceGetSetupStatusProcedure:
			setupServiceGetSetupStatusHandler.ServeHTTP(w, r)
		case SetupServiceApplySetupProcedure:
			setupServiceApplySetupHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSetupServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSetupServiceHandler struct{}

func (UnimplementedSetupServiceHandler) GetSetupStatus(context.Context, *emptypb.Empty) (*v1.GetSetupStatusResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("openmanet.setup.v1.SetupService.GetSetupStatus is not implemented"))
}

func (UnimplementedSetupServiceHandler) ApplySetup(context.Context, *v1.ApplySetupRequest, *connect.ServerStream[v1.ApplySetupResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("openmanet.setup.v1.SetupService.ApplySetup is not implemented"))
}
